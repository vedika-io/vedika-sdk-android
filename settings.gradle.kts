rootProject.name = "vedika-android-sdk"

// Single-module project: the client lives directly at the root
// (build.gradle.kts), mirroring sdks/flutter's single-package layout.
// No `include(...)` needed for a one-module build.
